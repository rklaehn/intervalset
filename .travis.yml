sudo: false
language: scala
scala:
  - "2.12.1"
script:
  - sbt clean coverage intervalsetJVM/test coverageReport
after_success:
  - bash <(curl -s https://codecov.io/bash)
  - sbt coveralls
branches:
  only:
    - master
cache:
  directories:
    $HOME/.ivy2/cache
